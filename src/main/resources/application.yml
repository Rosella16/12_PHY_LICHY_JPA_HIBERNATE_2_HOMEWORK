spring:
  application:
    name: homework2
  datasource:
    url: jdbc:postgresql://localhost:5432/jpa
    username: postgres
    password: rosella22
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        spring:
          jpa:
            show-sql=true:
